# Evidence layer files

`pulse-studies.js` is the browser-local runtime.

`validate-study-evidence.js` is a deterministic CI guardrail for polarity semantics.

`browser-tests/pulse-study-intelligence.spec.js` validates provenance, claims, contradiction flags, deduplication, context injection, and evidence removal.
