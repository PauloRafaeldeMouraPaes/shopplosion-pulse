const { test, expect } = require('@playwright/test');
test.describe('Universo: sinais, diagnóstico e oportunidade',()=>{
 test('todas as evidências aparecem sem filtro',async({page})=>{await page.goto('/index.html?v=20260920.04');const total=await page.evaluate(()=>window.PULSE_EVIDENCE.length);await expect(page.locator('#pv3-signals .pv4-evidence')).toHaveCount(total)});
 test('filtro por categoria reduz o canvas',async({page})=>{await page.goto('/index.html?v=20260920.04');const sel=page.locator('#pv3-category');const options=await sel.locator('option').evaluateAll(xs=>xs.map(x=>x.value).filter(Boolean));expect(options.length).toBeGreaterThan(0);const category=options[0];await sel.selectOption(category);const expected=await page.evaluate(c=>window.PULSE_EVIDENCE.filter(x=>x.categoria===c).length,category);await expect(page.locator('#pv3-signals .pv4-evidence')).toHaveCount(expected);});
 test('diagnóstico e oportunidade existem',async({page})=>{await page.goto('/index.html?v=20260920.04');await expect(page.locator('.pv4-diagnostic')).toBeVisible();await expect(page.locator('.pv4-opportunity')).toBeVisible();await expect(page.locator('.pv4-opportunity')).toContainText('Onde crescer')});
});