name: Pulse Stage 1 (retired)
on:
  push:
    branches:
      - feat/pulse-5-evolucoes-2026-08-26
jobs:
  retired:
    if: ${{ false }}
    runs-on: ubuntu-latest
    steps:
      - run: echo 'Stage 1 is complete; no rerun.'
